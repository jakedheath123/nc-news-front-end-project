import React from "react";

const TopicCard = props => {
  const {
    title,
    body,
    created_at,
    votes,
    author,
    comment_count
  } = props.article;
  return <div></div>;
};

export default TopicCard;
